# PO-Checkliste `18.0.0` / Sideload `18.0.1`

Download: https://github.com/Gamerio2904/Jarvis/raw/main/releases/Jarvis.apk

versionName **`18.0.1`**, versionCode **`180001`**.

Altes zuerst deinstallieren. Hausstand vorher exportieren (Einstellungen → Export).

Diese APK liefert die **Kugel** (Sprints 268–269) plus den Lage-Tab-Fix:
Kugel darf den Schirm nicht schwarz einfrieren. `globe.gl` (Sprint 270) ist
**nicht** gezogen. Schach-Modus, Chat-Blöcke und Screenshot-Fixes 260–267
bleiben PLAN — die Kästen unten, die das verlangen, sind bewusst als
„noch nicht“ markiert.

---

## 1. Installieren und Version

1. APK herunterladen.
2. Altes Jarvis deinstallieren, neu installieren, Keys eintragen.
3. Tippe:

```
Was kannst du?
```

In der Antwort muss **`18.0.1`** stehen.

Lage-Tab antippen: Kugel sichtbar, **kein** schwarzer Vollschirm. Chat, Composer
und **Lage aus** bleiben erreichbar.

---

## 2. Kugel — Tag/Nacht und ISS (268)

```
Zeig die Erde
```

Kugel auf. Tag- und Nachtseite erkennbar, ISS als **Bahn** plus Punkt.
**Kein** Label „Live“.

Lage verlassen: Animation pausiert.

---

## 3. Schichten auf Zuruf (269)

Ohne Extra-Satz: keine Erdbeben-/Feuer-Wolke (ISS darf bleiben).

```
Zeig Erdbeben
```

Marker + Text mit USGS und Alter. Kein „Live“. **Keine** Kugel als Ort
„Erdbeben“.

```
Wo brennt es
```

Marker + NASA EONET **oder** ehrliches „Quelle antwortet nicht“.

```
Was fliegt über uns
```

Wenige Flugzeuge um den Standort (sonst Deutschland-Mitte) + OpenSky und Alter.
**Oder** ehrlich leer.

Flugmodus: kein hängender Spinner.

---

## 4. Noch PLAN (260–267) — nicht an dieser APK prüfen

Diese Sätze aus den Screenshots vom 14.9.2026 sind **nicht** der Lieferumfang
dieser `18.0.0`-Kugel-APK:

- `Lass uns Schach spielen`
- `Bauer e2 e4`
- `Zeig mir das Schachbrett`
- `Wie steht die Bundesliga?` als Karte
- Wahlergebnis Sachsen-Anhalt / Satzabbruch `volatil. bis eine`

---

## 5. Won’t

Kein EarthOS, kein Cesium, kein `globe.gl` in der APK. Kein zweiter
LLM-Organizer.
