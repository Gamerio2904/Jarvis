# TEST 18.13 — Mister-Beats-Clips (PLAN)

Nach Execute von [`84-next.md`](./84-next.md). App-Code Ziel **`18.13.0`**.
Heute Sideload **`18.12.0`** — diese Datei ist die Abnahme, kein grüner Lauf.

## 1. Version

Einstellungen / Hilfe nennt **`18.13.0`**. Nicht `18.5.0`.

## 2. Ankommen

Galerie → Teilen → Jarvis (Android) oder Datei `video/*`.
Clip liegt im Vault, Status queued. Kein OCR-Text, kein Post.

Web: ehrlich „Teilen nur in der Android-App“.

## 3. Ein Agent

`Lade den Clip auf Instagram` trifft `clip`, nicht `doc`, nicht `hud`,
nicht `food`. `Öffne Instagram` bleibt wont.

Kein zweiter Agent „Beschreibung“ oder „Account“.

## 4. Caption + Rechte

Jarvis schlägt eine Zeile vor, erfindet keinen Song und keinen Handle.
Nutzer ändert, dann `Ja`. Ohne Ja kein Upload.

Erste Veröffentlichung: Rechte-Frage. Jarvis sagt nicht „ist legal“.

## 5. Instagram

Ohne Token / Review: ehrlich aus, Datei bleibt.

Mit Testkonto: Reels-Container, rupload, `media_publish` nach Ja.
Handle nur der in Settings. Quota von `content_publishing_limit`.
Danach Datei weg, Metadaten bleiben.

## 6. 10 und 5

Vault 12 → nach Publish 10: eine Notify „noch 10 Clips“.
Weiter auf 5: eine Notify „noch 5“.
Auffüllen auf 15: keine Notify.
12→4 in einem Schritt: beide.

## 7. Beides

`Beides`: Instagram + TikTok. Inbox-Upload ist nicht „online“.
Datei bleibt, bis beide bestätigt sind.

## 8. Kein Diebstahl

`Staffel 6 Folge 3` bleibt hud. `Zutaten von Nutella` bleibt food.
Kein inoffizieller Instagram-Client.
